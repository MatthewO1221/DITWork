
#include "ModularNumbers.hpp"


std::string Decrypt(std::string encrypted, int shiftBy)
{

	std::string decrypted;


	for each (auto character in encrypted)
	{
		if (std::isalpha(character))
		{

		}
		char newCharacter = 
	}
}
